const form=document.querySelector('#reset-password-form');const button=form.querySelector('button[type="submit"]');const error=document.querySelector('#reset-error');const token=new URLSearchParams(location.search).get('token')||'';document.querySelectorAll('[data-toggle-password]').forEach(toggle=>toggle.addEventListener('click',()=>{const input=toggle.parentElement.querySelector('input');input.type=input.type==='password'?'text':'password';toggle.textContent=input.type==='password'?'Show':'Hide'}));if(!token)error.textContent='This reset link is invalid or incomplete. Request a new reset link.';form.addEventListener('submit',async event=>{event.preventDefault();error.textContent='';const password=form.password.value;const confirmPassword=form.confirmPassword.value;if(!token){error.textContent='This reset link is invalid or incomplete. Request a new reset link.';return}if(password.length<12){error.textContent='Use a password with at least 12 characters.';return}if(password!==confirmPassword){error.textContent='Passwords do not match.';return}button.disabled=true;button.querySelector('span').textContent='Updating…';try{const response=await fetch('/api/auth/reset-password',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({token,password,confirmPassword})});const data=await response.json().catch(()=>({}));if(!response.ok)throw new Error(data.error||'Password could not be reset.');location.replace(data.redirectTo||'/login?passwordReset=1')}catch(cause){error.textContent=cause.message;button.disabled=false;button.querySelector('span').textContent='Update password'}})
