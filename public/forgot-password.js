const form=document.querySelector('#forgot-password-form');const button=form.querySelector('button');const error=document.querySelector('#forgot-error');const result=document.querySelector('#forgot-result');form.addEventListener('submit',async event=>{event.preventDefault();error.textContent='';result.hidden=true;button.disabled=true;button.querySelector('span').textContent='Sending…';try{const response=await fetch('/api/auth/forgot-password',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:form.email.value.trim()})});const data=await response.json().catch(()=>({}));if(!response.ok)throw new Error('We could not process this request. Please try again shortly.');result.textContent=data.message||'If an account exists for that email, we’ve sent password reset instructions.';result.hidden=false;form.reset()}catch(cause){error.textContent=cause.message}finally{button.disabled=false;button.querySelector('span').textContent='Send reset link'}})
