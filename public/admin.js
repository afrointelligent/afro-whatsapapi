function escapeHtml(value){const node=document.createElement('span');node.textContent=value||'';return node.innerHTML}
async function reviewDocument(button){const label=button.textContent;button.disabled=true;button.textContent='Updating…';const response=await fetch(`/api/admin/verification-documents/${button.dataset.review}`,{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({status:button.dataset.value})});if(!response.ok){button.disabled=false;button.textContent=label;alert('Could not update internal onboarding status.');return}await loadAdmin()}
async function loadAdmin(){const response=await fetch('/api/admin/verification-documents');if(response.status===401){location.replace('/login');return}if(response.status===403){document.querySelector('#admin-status').textContent='This account is not a platform administrator.';return}const data=await response.json(),items=data.documents||[],summary=data.summary||{};document.querySelector('#admin-status').textContent=`${items.length} private onboarding document${items.length===1?'':'s'} available for internal review.`;let summaryNode=document.querySelector('#admin-summary');if(!summaryNode){summaryNode=document.createElement('div');summaryNode.id='admin-summary';summaryNode.className='admin-summary';document.querySelector('#admin-documents').insertAdjacentElement('beforebegin',summaryNode)}summaryNode.innerHTML=`<article><b>${summary.businesses||0}</b><span>Businesses</span></article><article><b>${summary.approved||0}</b><span>Internally approved</span></article><article><b>${summary.replacementRequired||0}</b><span>Need more information</span></article><article><b>${summary.underReview||0}</b><span>Under review</span></article>`;document.querySelector('#admin-documents').innerHTML=items.length?items.map(item=>{const approved=['APPROVED_FOR_META_ONBOARDING','SUBMITTED_TO_META'].includes(item.tenantVerificationStatus);const displayStatus=approved?'AFROINTELLIGENT_APPROVED':item.status;return `<article><div class="document-info"><div class="document-heading"><p class="tenant">${escapeHtml(item.tenantName||'Workspace')}</p><span class="status-pill status-${escapeHtml(displayStatus.toLowerCase())}">${escapeHtml(displayStatus.replaceAll('_',' '))}</span></div><h2>${escapeHtml(item.filename)}</h2><p>${escapeHtml(item.type.replaceAll('_',' '))} · ${escapeHtml(item.purpose)}</p><small>${new Date(item.uploadedAt).toLocaleString()}</small></div><div class="review-actions"><a href="/api/admin/verification-documents/${item.id}/download">↓ Download securely</a><div class="review-buttons"><button class="approve" data-review="${item.id}" data-value="READY_FOR_VERIFICATION" type="button" ${approved?'disabled':''}>${approved?'✓ AfroIntelligent approved':'✓ Approve onboarding'}</button><button class="replace" data-review="${item.id}" data-value="REPLACEMENT_REQUIRED" type="button">Request more information</button><button class="reject" data-review="${item.id}" data-value="REJECTED" type="button">Reject onboarding</button></div></div></article>`}).join(''):'<div class="empty">No uploaded documents yet.</div>';document.querySelectorAll('[data-review]').forEach(button=>button.addEventListener('click',()=>reviewDocument(button)))}
document.querySelector('#admin-logout').addEventListener('click',async()=>{await fetch('/api/auth/logout',{method:'POST'});location.assign('/')});loadAdmin()
